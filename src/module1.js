import 'lodash'

export default {
    sayName(name) {
        let fenghao = 'fenghao'
        document.write('My name is' + name);
    },
    sayColor(color) {
        document.write('My name is' + color)
    }
}